Reduced, analysis-ready data products grouped by galaxy. READMEs in each directory detail the contents.


*See [QUESTDwarfs/fit_outputs](https://github.com/QUESTDwarfs/fit_outputs) for `Bagpipes` and `pysersic` posteriors and fits, as well as `GALFIT` fits and models (where files are sufficiently small for GitHub LFS).*